# VisualStudioSolutionMerger
Handles merges in Visual Studio's SLN files. Now with a web version that also contains the features of [the differ](https://github.com/GuiRitter/VisualStudioSolutionDiffer).
